@model LectorCreateViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Lector</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            @*<div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <label asp-for="GebruikerId" class="control-label"></label>
            <input asp-for="GebruikerId" class="form-control" />
            <span asp-validation-for="GebruikerId" class="text-danger"></span>
        </div>*@
            <div class="form-group">
                <label asp-for="GebruikerId">Tekomstige Lector</label>
                <select asp-for="GebruikerId" class="form-control">
                    <option value="">Please Select</option>
                    @foreach (Gebruiker gebruiker in Model.Gebruikers)
                    {
                        <option value="@gebruiker.GebruikerId">@gebruiker.GebruikerId : @gebruiker.Naam @gebruiker.VoorNaam</option>
                    }
                </select>
                <span asp-validation-for="GebruikerId" class="text-danger"></span>
            </div>
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
