@model IEnumerable<Handboek>

@{
    ViewData["Title"] = "Handboeken";
}

<p>
    <a asp-action="Create">Create New</a>
</p>
<div class="boeken custom-container">
    @foreach (Handboek handboek in Model)
    {
        <partial name="HandboekCard" model="@handboek" />
    }
</div>
